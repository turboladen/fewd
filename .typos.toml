[default]
extend-ignore-re = [
  "uuid",  # UUIDs often flagged as typos
]
extend-words = { fewd = "fewd", parm = "parm" }  # Project name; "parm" = colloquial parmesan in recipe text

[files]
extend-exclude = [
  "target/",
  "node_modules/",
  "dist/",
  "*.db",
  "bun.lock",
  "Cargo.lock",
]
